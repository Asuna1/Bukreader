version: '2'
services:
    bugreader-mongodb:
        container_name: bugreader-mongodb
        image: mongo:3.3.12
        ports:
            - "27017:27017"
        command: mongos --configdb bugreader-mongodb-config
    bugreader-mongodb-node:
        build:
            context: .
            dockerfile: mongodb/MongoDB.Dockerfile
        command: mongod --replSet rs1 --noprealloc --smallfiles
    bugreader-mongodb-config:
        image: mongo:mongo:3.3.12
        container_name: bugreader-mongodb-config
        command: mongod --noprealloc --smallfiles --configsvr --dbpath /data/db
